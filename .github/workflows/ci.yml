<<<<<<< HEAD
name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install Python dependencies
        run: |
          cd backend
          pip install -r requirements.txt

      - name: Python syntax check
        run: |
          python -m py_compile backend/server.py
          python -m py_compile backend/providers.py
          python -m py_compile backend/memory.py
          python -m py_compile backend/agent_loop.py
          python -m py_compile backend/config.py
          python -m py_compile backend/keystore.py
          python -m py_compile backend/tools.py
          python -m py_compile backend/workspace.py
          python -m py_compile backend/agents/orchestrator.py
          python -m py_compile backend/agents/planner.py
          python -m py_compile backend/agents/architect.py
          python -m py_compile backend/agents/coder.py
          python -m py_compile backend/agents/reviewer.py
          python -m py_compile backend/agents/tester.py

      - name: Install frontend dependencies
        run: |
          cd desktop
          npm install

      - name: TypeScript check
        run: |
          cd desktop
          npx tsc --noEmit || true

      - name: Frontend build
        run: |
          cd desktop
          npm run build || true
=======
# .warpfix.yaml — WarpFix configuration
version: 1

repair:
  auto_fix: true                  # Enable automatic fix PRs
  confidence_threshold: 0.75      # Minimum AI confidence to open PR (0.0-1.0)
  max_retries: 2                  # Max repair attempts per failure
  sandbox_timeout: 300            # Sandbox validation timeout (seconds)

quality_gates:
  min_confidence: 0.8             # Block PRs below this confidence
  require_tests: true             # Require test pass in sandbox
  max_patch_lines: 50             # Max lines changed per fix
  block_on_security: true         # Block fixes with security concerns

review:
  auto_comment: true              # Comment on existing PRs with analysis
  severity_labels: true           # Add severity labels to issues
  mermaid_diagrams: true          # Include architecture diagrams
  effort_estimates: true          # Estimate fix effort in hours

ignore:
  paths:
    - "node_modules/**"
    - "dist/**"
    - "*.lock"
  errors:
    - "ECONNRESET"                # Ignore transient network errors
    - "rate limit"                # Ignore rate limiting

notifications:
  slack:
    channel: "#ci-repairs"
    on_repair: true
    on_failure: true
  email:
    to: "team@yourcompany.com"
    on_security: true
>>>>>>> fe1ee6fc5fc952193f16b34c47df88e5d2d36df4
